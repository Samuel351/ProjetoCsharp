@model ProjetoASPNetCore.Models.ViewModels.ErrorViewModel

<br />
<h2 class="text-danger">@Model.Message</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Requisição ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<a asp-action="Index" class="btn btn-info">Retornar</a>
